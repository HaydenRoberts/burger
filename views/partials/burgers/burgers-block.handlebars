<li>
	{{burger_name}}

	<button class="devoured" data-id="{{id}}" data-burger="{{devoured}}">
		{{#if devoured}}YUM YUM!{{else}}EAT ME!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
